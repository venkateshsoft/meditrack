import type { Frequency, Medication, MedicationDose } from '../models/types'
import { id } from '../services/storage'
export const defaultTimes:Record<Frequency,string[]>={'Once daily':['09:00'],'Twice daily':['09:00','21:00'],'Three times daily':['08:00','14:00','20:00'],'Four times daily':['06:00','12:00','18:00','22:00'],'Every 6 hours':['06:00','12:00','18:00','00:00'],'Morning':['08:00'],'Afternoon':['14:00'],'Evening':['18:00'],'Bedtime':['22:00']}
export function scheduleFor(m:Medication):MedicationDose[]{const output:MedicationDose[]=[]; const start=new Date(m.startDate+'T00:00:00'); for(let d=0;d<m.durationDays;d++){const day=new Date(start);day.setDate(day.getDate()+d); for(const time of m.reminderTimes){const [h,min]=time.split(':').map(Number);const at=new Date(day);at.setHours(h,min,0,0);output.push({id:id(),medicationId:m.id,scheduledTime:at.toISOString(),status:'PENDING'})}} return output}
export function endDate(start:string,days:number){const [year,month,day]=start.split('-').map(Number);const d=new Date(year,month-1,day);d.setDate(d.getDate()+Math.max(0,days-1));return [d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')}
export function allergyMatch(med:Pick<Medication,'name'|'genericName'>, allergies:{name:string}[]){const names=[med.name,med.genericName||''].map(x=>x.toLowerCase().trim());return allergies.find(a=>names.some(n=>n.includes(a.name.toLowerCase())||a.name.toLowerCase().includes(n))) }
